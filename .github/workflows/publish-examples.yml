name: Publish Examples

on:
  push:
    branches: [ "master" ]

jobs:
  build:

    runs-on: [ self-hosted ]

    steps:
    - uses: actions/checkout@v3

    - name: Install dependencies
      run: make setup

    - name: Build DataVis
      run: make datavis

    - name: Build tests
      run: make tests

    - name: Publish examples
      run: make tests-publish
